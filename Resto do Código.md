**Resto do Código**

tk.Button(janela, text="Recomeçar", command=recomecar).grid(row=5, column=0, columnspan=2)

tk.Button(janela, text="Novo Tabuleiro", command=novo\_tabuleiro).grid(row=5, column=2, columnspan=2)



janela.mainloop()

